<h1>Hello <%= @user.name || @user.email%>!</h1>

<h1><%= link_to "Edit Profile", edit_user_registration_path %></h1>

<h1>Your To-do lists</h1>


<%= render partial: 'items/items', collection: @items %>
<%= render partial: 'items/form', locals: { item: @user.items.new, user: @user } %>

